# Deferred Work

Append-only. New entries are appended by step-04 of the bmad-build workflow when a review
finding is classified as `defer` (a pre-existing issue surfaced incidentally, not caused by
the current story). Each entry's `source_spec` is the story that surfaced it.

<!-- Append new entries below this line. Do not modify existing entries. -->

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-apply-strict-csp-via-nextconfigjs-and-experimentalsri.md`
  summary: Automated regression tests for CSP header presence, directive completeness, SRI mechanism on emitted HTML, and absence of proxy.ts/middleware.ts belong in CI, not in this platform-config story.
  evidence: Verification Gap Reviewer surfaced 4 findings — none of them are implementation gaps in story 1-2, but all are real and unverified by automation. The CI gate work is owned by story 1-4 per sprint-status.yaml ("set up github actions ci with lighthouse + gzip-budget + route-invariant + pa11y-ci gates"). Story 1-2 establishes the platform-level configuration; story 1-4 will assert it does not regress on every PR.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-apply-strict-csp-via-nextconfigjs-and-experimentalsri.md`
  summary: `style-src 'self'` will break any future inline-style usage (e.g., shadcn dialog overlay opacity, signature-canvas dynamic SVG fill colors); the spec needs `style-src-elem` and `style-src-attr` directives — or a documented deferral to E1.5.
  evidence: Blind Hunter finding flagged that the current CSP conflates stylesheet links (`<link rel="stylesheet">`) with inline `<style>` blocks and `style="…"` attributes. With Tailwind v4 + `experimental.sri`, the stylesheet path is handled by SRI, but if any future client component sets an inline style (common for shadcn overlays), the browser will block it. Story 1-5 ("set up app/globals.css with the closed 57-token @theme block") is the natural owner of this decision; the CSP must be re-stated when the real layout first mounts inline styles.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-apply-strict-csp-via-nextconfigjs-and-experimentalsri.md`
  summary: `Cache-Control: s-maxage=31536000` on prerendered responses means a CSP change takes up to a year to propagate to cached edges — `Cache-Control: no-store` for the CSP header itself, or a revalidation hook on CSP changes, needs to be designed.
  evidence: Blind Hunter finding flagged that the static-first invariant (AD-13) produces `s-maxage=31536000` on every prerendered response, including the headers. If a future CSP amendment is needed (e.g., adding a new analytics endpoint), the change will not propagate to users hitting cached edges for up to a year. This is a deployment/CI concern, not a story 1-2 concern; it should be addressed by the E1.4 CI + cache-invalidation workflow or a dedicated header-cache story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-apply-strict-csp-via-nextconfigjs-and-experimentalsri.md`
  summary: The future `connect-src 'self' https://plausible.io https://api.resend.com` allowlist must be re-validated against actual outbound calls in E4 (forward-to-hiring-manager endpoint) and E5a (subscribe form); missing endpoint at runtime = silent failure of analytics or form submission.
  evidence: Blind Hunter finding flagged that the CSP `connect-src` allowlist in this story is *forward-looking* — neither Plausible nor Resend are wired in story 1-2. If a later story adds an outbound endpoint not on this list (e.g., a different email provider, a CMS, an error reporter), the browser will silently block the call. This is correctly designed to be future-proof, but the runtime assertion must live in the story that first makes the call (4-3 for Resend, 5a-7 for the subscribe form).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-apply-strict-csp-via-nextconfigjs-and-experimentalsri.md`
  summary: `frame-src 'self' https://*.sanjit.dev` subdomain wildcard needs to be enumerated as a closed list before E4.5 (recruiter-route OG metadata); if E4.5 adds a new subdomain (e.g., `pdf.sanjit.dev`) the wildcard still allows it, but the closed-list discipline says "every subdomain must be named".
  evidence: Blind Hunter finding flagged that the wildcard is currently intentional (the OG image endpoint and PDF preview will live on `*.sanjit.dev`), but AD-12 closed-list discipline demands the list be enumerated at the moment of first use. E4.5 is the natural owner (it builds the OG image at `og.sanjit.dev` or similar); when that story ships, the wildcard should be replaced with the explicit subdomain list.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-wire-proxyts-at-project-root-next-16-convention-not-middlewarets.md`
  summary: Automated regression tests for the `proxy.ts` 308 redirect (status code, `Location:` header shape, `for`-deletion, pass-through behavior, file-tree invariant) belong in CI, not in this proxy story.
  evidence: Verification Gap Reviewer flagged that the spec's Verification section is entirely manual (`pnpm install` + `pnpm build` + 4 manual `curl -sI` checks + file-tree `ls`). No automated test will catch a regression to 307 instead of 308, a wrong path rewrite (`/recruiter/` trailing slash), an accidental `for`-reintroduction, or a future re-creation of `middleware.ts`. Story 1-4 ("set up GitHub Actions CI with Lighthouse, gzip-budget, route-invariant, and pa11y-ci gates") is the natural owner — its `route-invariant` check could grow a sub-assertion for the proxy's 308 behavior. Story 1-3 establishes the platform-level proxy; story 1-4 will assert it does not regress on every PR.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-wire-proxyts-at-project-root-next-16-convention-not-middlewarets.md`
  summary: A CI-side scan (lint rule, pre-commit hook, or CI grep) that fails the build if `proxy.ts` or `middleware.ts` ever reintroduces a nonce/`'unsafe-inline'`-based CSP injection should live alongside the other AD-11 boundaries.
  evidence: Adversarial Blind Hunter finding flagged that AC #7 ("no `nonce-` or `'unsafe-inline'` in `proxy.ts`") is a negative assertion with no runner — only a human `grep` exercises it today. The same boundary holds for `middleware.ts` (must not exist) and `next.config.mjs` (already owned by E1.2). A single shared lint or CI check that fails on `nonce-` in any proxy/middleware-shaped file would protect the AD-11 contract across future edits. Story 1-4 (CI gates) is the natural home for this assertion.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-bootstrap-nextjs-16-vercel-deploy.md`
  summary: `pnpm typecheck` and `pnpm lint` are bound as AC4/AC5 contracts but the wired CI workflow (`.github/workflows/ci.yml`) does not run either — local-only verification is the only enforcement today.
  evidence: Verification Gap Reviewer read `.github/workflows/ci.yml` in full (81 lines). The `gate` job runs `pnpm install --frozen-lockfile`, `pnpm build`, `pnpm audit:spec`, server boot + curl probe, `pnpm lighthouse`, `pnpm audit:budget`, `pnpm audit:routes`, `pnpm test:a11y`. Neither `pnpm typecheck` (AC4) nor `pnpm lint` (AC5) is invoked. Today a contributor who adds an unused variable or an `any`-cast to `app/` will not fail CI on the typecheck or lint axis — only on the build/runtime axis. Verified at HEAD by running `pnpm typecheck` (exits 0 clean for placeholder files) and `pnpm lint` (currently exits 1 due to a separate, unrelated `require()`-import error in `scripts/pa11y-html-reporter.js` introduced by story 1-4; that 1-4 issue is not in scope here). Story 1-4 owns the CI gate surface and is the natural home for adding two steps after the existing `Build` step: `pnpm typecheck` and `pnpm lint` (the latter with `--max-warnings=0` to convert warnings to failures and honor AC5's "zero errors" contract).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-bootstrap-nextjs-16-vercel-deploy.md`
  summary: AC6 (pinned versions, `engines.node >=20.9.0`, no `tailwind.config.ts`) and AC7 (no `proxy.ts`, no `middleware.ts`) are file-tree / manifest invariants with no automated runner; a CI-side check should fail the build on any drift.
  evidence: Verification Gap Reviewer read `.github/workflows/ci.yml` and `scripts/audit-routes.spec.mjs` (the only `.spec.mjs` in the repo) and confirmed no script asserts the package.json pin enumeration, the `engines.node` floor, the absence of `tailwind.config.{js,ts}`, or the presence/absence of `proxy.ts` / `middleware.ts`. `pnpm install --frozen-lockfile` catches lockfile drift (partial AC1 coverage) but a manual pin downgrade (`"zod": "^3.23.8"`) would not fail CI. AC7 is particularly fragile: introducing a `middleware.ts` would still let `pnpm build` exit 0 (Next 16 emits a warning, not a failure) and no test would catch it. Story 1-4's CI gate is the natural owner; the implementation is a small shell or `node` script that greps the repo for the closed sets, similar to `scripts/audit-routes.spec.mjs` which already enforces a closed-list invariant (`PROOF_NUMBERS`).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-bootstrap-nextjs-16-vercel-deploy.md`
  summary: On a fresh clone where `node_modules/` is installed but `.next/` has never been built, `pnpm typecheck` will fail because `next-env.d.ts` imports `./.next/dev/types/routes.d.ts` and `./.next/dev/types/root-params.d.ts`, which Next only generates during `next dev` / `next build`.
  evidence: Verification Gap Reviewer + Blind Hunter both flagged this latent ordering risk. Ran `pnpm typecheck` at the current HEAD (where `.next/` already exists from prior builds) and confirmed it exits 0. The risk surfaces only on the first command after a clean checkout: `pnpm install && pnpm typecheck` would fail with `Cannot find module '.next/dev/types/routes.d.ts'`, requiring `pnpm dev` or `pnpm build` to run first. The spec's Verification commands already begin with `pnpm install --frozen-lockfile` and end with `pnpm typecheck` / `pnpm lint`, but does not mandate `pnpm build` before `pnpm typecheck`. Two low-friction fixes exist: (a) reorder Verification to put `pnpm build` before `pnpm typecheck` and `pnpm lint`; (b) drop the two `import` lines from `next-env.d.ts` (they are auto-regenerated on the next `next dev`/`next build`). Either fix belongs in a future spec amendment, not a story 1-1 loopback.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-set-up-appglobalscss-with-the-closed-57-token-theme-block-ad-18.md`
  summary: `.gitignore` shipped a latent Python-ecosystem `lib/` ignore rule that silently blocked `lib/design-tokens.ts` (and would have blocked story 1-6's `lib/canvas-modes.ts`); fixed in this story with `!lib/` + `!lib/**/*` allow-list but a project-wide `.gitignore` audit story is needed to enumerate all Python bleeding-in-Node rules (`lib64/`, `parts/`, `sdist/`, `var/`, `wheels/`, `develop-eggs/`, `downloads/`, `eggs/`, `.eggs/`, `share/python-wheels/`) and decide which to keep or drop.
  evidence: Verification Gap Reviewer (step-04 iteration 1) flagged that the Python-section `lib/` rule on `.gitignore:162` plus the project-designated `lib/` co-source directory is a fragile pattern. Without the fix the audit-tokens AC #3 (`DESIGN_TOKENS.length === 21`) would pass in CI but the underlying file would be untracked on a fresh clone. The fix is inline at `.gitignore:248-256` with a dated comment.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-set-up-appglobalscss-with-the-closed-57-token-theme-block-ad-18.md`
  summary: The `TOKEN_VALUE_DRIFT` I/O matrix gap (`audit:tokens` does not catch hex drift inside `app/globals.css` because the CSS file is not in `AUDIT_TOKEN_GLOBS`) is documented in the spec as "spine amendment + human review per AD-16"; a future spine-amendment AD-16 surrogate could be a `scripts/audit-token-values.mjs` (or an extension to `audit-routes.spec.mjs`) that parses `app/globals.css` @theme color declarations and asserts each equals the matching value from DESIGN.md.
  evidence: Verification Gap Reviewer + Edge Case Hunter both flagged this gap. `app/globals.css` is intentionally never walked (it's the source of truth, not a consumer). The closed allowlist is enforced at the consumer side but the canonical source itself has no value-axis check. Demonstrable regression: `sed -i 's/--accent: #A78BFA/--accent: #000000/' app/globals.css` passes every AC verification command in the spec today.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-set-up-appglobalscss-with-the-closed-57-token-theme-block-ad-18.md`
  summary: The `TOKEN_NAME_DRIFT` gap between `lib/design-tokens.ts` (21-entry `DESIGN_TOKENS` `as const` array) and `app/globals.css` (21 `@theme` color declarations) has no automated guard today — `audit-tokens` does not check token-name presence, only inline-hex absence. The first runtime signal arrives when story 1-6's `lib/canvas-modes.ts` imports a name that doesn't exist in `@theme` (a TypeScript compile error).
  evidence: Verification Gap Reviewer flagged that no AC verification command asserts `DESIGN_TOKENS.length === 21` against the @theme block's name axis. The verification is a one-liner (`npx tsx -e "import {DESIGN_TOKENS} ..."  console.log(...)`) but it is manual, not enforced in CI. A small Node ESM spec (parallel to `scripts/audit-routes.spec.mjs`) that parses both files and asserts the name sets are equal would close the gap; belongs to a future CI-hardening story.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-implement-the-10-closed-client-components-ad-13.md`
  summary: The 3 simple-stub client components (`ScrollProgress`, `SkipToContent`, `NavCurrent`) from the AD-13 closed set are deferred to a follow-up story; the 7 detailed components ship in 1-6.
  evidence: Step-02 token-count check showed the full 1-6 spec weighs ~5,600 tokens — ~3.5× the 1,600-token budget — primarily because each of the 10 components is enumerated with full implementation contracts. The 3 stubs have minimal contracts (ScrollProgress: passive scroll listener + scaleX; SkipToContent: sr-only + focus:not-sr-only anchor; NavCurrent: aria-current switch) and add line-count without adding decisions. Splitting them out preserves the single goal of "establish the closed-set invariant" without inflating the spec past the context-rot threshold for the implementer. The AD-13 closed-set invariant is preserved by a comment in `components/client/.gitkeep` or by listing the deferred 3 in the new spec's "Ask First" section.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-implement-the-10-closed-client-components-ad-13.md`
  summary: The closed-set invariant `ls components/client/` → exactly 7 files has no automated CI gate; a future PR adding an 8th component or renaming one slips through build/typecheck/audit-tokens untouched.
  evidence: Verification Gap Reviewer (step-04 iteration 1) confirmed no script in `scripts/` enumerates the closed-set name list. `scripts/audit-routes.spec.mjs` enforces a different closed set (`PROOF_NUMBERS`) but nothing enforces the AD-13 client-component set. Belongs to a future CI-hardening story; the implementation is a small node script that reads `components/client/`, sorts the names, and asserts the set equals the verbatim closed list (mirrors the existing `PROOF_NUMBERS` test).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-implement-the-10-closed-client-components-ad-13.md`
  summary: Preview-route aria/motion contracts (44px tap target on `<FilterChipGroup>`, 4 rows + hover callback on `<LayerRowHover>`, reduced-motion collapse on `<SignatureCanvas>`) have no automated verification — `.pa11yci.json` does not include `/__preview/filter-chip-group` or `/__preview/layer-row-hover`, and pa11y-ci cannot simulate `prefers-reduced-motion` or dispatch `Tab` keys.
  evidence: Verification Gap Reviewer + Adversarial Reviewer both flagged this. The CI workflow `.github/workflows/ci.yml` runs `pnpm test:a11y` (pa11y-ci) against the 10 production routes from `AUDIT_ROUTES`; the two preview routes are not in `.pa11yci.json`. A regression in the `aria-checked` attribute on the selected chip or in the row count would land green on CI. Belongs to a future CI-hardening story: extend `.pa11yci.json` to include the two preview URLs + add a node script that greps the rendered HTML for the structural assertions.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-implement-the-10-closed-client-components-ad-13.md`
  summary: Runtime motion-gate behavior (`matchMedia('(prefers-reduced-motion: reduce)')` collapsing animations to 0.01ms) has no automated test; pa11y-ci does not exercise matchMedia, and no JSDOM-based unit test exists.
  evidence: Verification Gap Reviewer flagged this as a pre-existing CI gap (no JSDOM/vitest setup in the project yet). The motion-gate contracts are runtime behaviors that pa11y-ci's static-HTML analysis cannot observe. Belongs to a future CI-hardening story that introduces a JSDOM/vitest harness (parallel to `scripts/audit-routes.spec.mjs`) and asserts that under a mocked `matchMedia('(prefers-reduced-motion: reduce)').matches === true`, the SMIL `dur` attribute on the rendered SVG is `'0.01ms'`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-implement-the-10-closed-client-components-ad-13.md`
  summary: Runtime focus-trap on `<CommandPalette>` (Tab/Shift+Tab cycling) has no automated test; pa11y-ci does not dispatch Tab keys, and no JSDOM-based test exists.
  evidence: Verification Gap Reviewer + Adversarial Reviewer both flagged this. The focus trap's `e.preventDefault()` branches in `handleKeyDown` (Tab when active element is last; Shift+Tab when active element is first) are unverified. A future contributor could invert the `e.shiftKey` condition and CI would not catch it. Belongs to the same future CI-hardening story as the motion-gate gap (JSDOM-based test that fires `keydown` events and asserts `document.activeElement` cycles).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-implement-hero-plus-proofvectorcluster-for-the-homepage.md`
  summary: The `## Suggested Review Order` section (highest-leverage entry-point ordering for the step-04 code-review reviewer) was deferred to keep the spec under the 1,600 SCOPE STANDARD token budget; the inline replacement in the spec is a one-paragraph pointer to the three files and three entry points, to be expanded into the full reviewer ordering block when this story enters step-04 review.
  evidence: Step-02 token-count check on the 1-8 spec weighed ~2,150 tokens before the split — ~34% over the 1,600-token budget — primarily because each closed-set contract (AD-13 9-component closed set, AD-17 dual-ring canvas mount, AD-18 21-color token mirrors, AD-20 motion gate + 44px tap target, AD-13 closed 8-element PROOF_NUMBERS set) was cited with file:line anchors in the Verification + Design Notes + Suggested Review Order sections. The 1-7 spec follows the same closed-set citation discipline and weighs ~2,400 tokens; the cross-story pattern in this repo is to keep cohesive stories in one file. The reviewer ordering block is a step-04 convenience, not an execution-blocking section, so it survives a deferral to review-helper time without affecting the implementer's contract.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-implement-hero-plus-proofvectorcluster-for-the-homepage.md`
  summary: `app/__preview/` uses double-underscore prefix (`__preview/hero`) which Next.js 16 App Router treats as a private folder convention — every preview route is silently EXCLUDED from the build output and unreachable from `pnpm audit:routes` / pa11y-ci. Pre-existing across 1-6 (filter-chip-group, layer-row-hover), 1-7 (nav), and 1-8 (hero) — surfaced by step-04 review of story 1-8.
  evidence: Verification Gap Reviewer (step-04 of story 1-8) confirmed `pnpm build` output for story 1-8 lists only `○ /` and `○ /_not-found` despite `app/__preview/hero/page.tsx` existing on disk; `ls .next/server/app/` shows no `__preview/` directory. The 1-7 Nav preview (`app/__preview/nav/page.tsx`) is similarly absent from the build output, as are 1-6's `filter-chip-group` and `layer-row-hover` previews. Per Next.js 16 App Router convention, folders prefixed with underscore (`_folder/`) are private and unreachable from routing; the spec-1-7 page comment on `app/__preview/nav/page.tsx:25-26` claims `__preview` "avoids Next.js's `_components` private-folder convention" but the implementation never disabled the private-folder rule (no `experimental.disableRouteFiltering` or `app/**/_*` allowlist). The CI gate that should catch regressions on preview routes (`pnpm audit:routes`, `pnpm test:a11y`) never reaches them — every preview route's rendered-HTML contract (verifiable spine line, gradient-text climax, 4-item PROOF_NUMBERS membership, AD-20 tap-target, `min-h-[44px]`, `robots: noindex`) is invisible to CI. Renaming `__preview/` → `preview/` (or `__preview_` → `_preview/` per Next.js's documented underscore-prefix-with-underscore escape) is a routing-config change that affects 4 prior-story preview routes — out of scope for 1-8. Belongs to a future CI-hardening story: rename the `__preview/` folder convention (or add a Next.js config opt-out) AND add the renamed routes to `AUDIT_ROUTES` + `.pa11yci.json` so the existing rendered-HTML assertions (spine + proof + return path) cover the preview harnesses.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-implement-the-10-closed-client-components-ad-13.md`
  summary: The 9-mode `CANVAS_MODES` closed tuple has no CI assertion verifying it remains the verbatim list from `epic-1-context.md:29` (or against the order in `ARCHITECTURE-SPINE.md` AD-17). A rename like `'activity-feed'` → `'activityFeed'` would compile but silently break 1-11's `mode="activity-feed"` mount.
  evidence: Verification Gap Reviewer flagged this. `scripts/audit-routes.spec.mjs` enforces the `PROOF_NUMBERS` closed set via a verbatim grep + `assert.deepEqual`; no parallel assertion exists for `CANVAS_MODES`. Belongs to a future CI-hardening story that adds `scripts/audit-canvas-modes.spec.mjs` parsing `lib/canvas-modes.ts` and asserting the tuple equals the documented order.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-implement-the-10-closed-client-components-ad-13.md`
  summary: The `app/__preview/filter-chip-group` and `app/__preview/layer-row-hover` routes ship to the production build despite being "preview harnesses" — Next.js prerenders them at build time. The `__` (double-underscore) prefix is documentation-only, not enforced. Production robots can index them unless `metadata.robots = { index: false }` is set (which 1-6 just added in patch B5).
  evidence: Adversarial Reviewer (step-04 iteration 1) flagged that the preview routes would be publicly reachable at `https://sanjit.dev/__preview/...` without B5. With B5 applied (`robots: { index: false, follow: false }`), they are not indexed but are still served. A future contributor could accidentally remove the metadata export. Belongs to a future spine-concern story that either gates preview routes behind an env var (`if (process.env.PREVIEW !== '1') notFound()`) or moves them outside the app-router tree (e.g., to `app/__preview.disabled/` with the rename triggered by a build flag).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-implement-persistent-nav-with-brand-mark-links-command-palette-trigger-magnetic-cta-scroll-progress.md`
  summary: The 6-of-8 AC verification gap (only `pnpm build` and `pnpm audit:tokens` are CI-enforced; AC #1 9-file invariant, AC #3 `aria-current` derivation, AC #4 responsive visibility thresholds, AC #5 scroll + reduced-motion gate, AC #6 brand-mark gradient utility, AC #9 closed NAV_LINKS set — none are CI-asserted) belongs in a future CI-hardening story.
  evidence: Verification Gap Reviewer (step-04 iteration 1) confirmed no test file references `NavCurrent`, `ScrollProgress`, `Nav.tsx`, `__preview/nav`, or `prefers-reduced-motion` in any test context. The nav contract is CI-unverified; a regression removing `aria-current` derivation or the reduced-motion gate would land green on CI. Belongs to the same CI-hardening story as 1-5/1-6 CI gaps: add a vitest+testing-library render test for `<Nav>` and `<NavCurrent>`, add a node-script closed-set lock on `NAV_LINKS` (parallel to `scripts/audit-routes.spec.mjs` `PROOF_NUMBERS` lock), and add a JSDOM test for `<ScrollProgress>` asserting `--progress` writes under scroll and pinning under reduced-motion.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-implement-persistent-nav-with-brand-mark-links-command-palette-trigger-magnetic-cta-scroll-progress.md`
  summary: `app/__preview/nav/page.tsx` is not registered in `AUDIT_ROUTES` (the closed pa11y-ci + Lighthouse URL set in `scripts/audit-routes.shared.mjs`), so CI never scrapes or a11y-scans the preview harness — even though the spec line 23 says it is the verification surface.
  evidence: Verification Gap Reviewer (step-04 iteration 1) confirmed `.pa11yci.json` and the lighthouse job both iterate the 10 production routes from `AUDIT_ROUTES`; `__preview/nav` is excluded. This is the same shape as the 1-6 preview-route gaps already in deferred-work.md; consolidating under a single future-CI-hardening story that adds the preview routes to either `AUDIT_ROUTES` or a parallel `PREVIEW_ROUTES` set is the natural fix.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-implement-persistent-nav-with-brand-mark-links-command-palette-trigger-magnetic-cta-scroll-progress.md`
  summary: `<Nav>`'s `isCurrent` predicate does not canonicalize `currentPath` (trailing slash, query string, hash fragment, locale prefix). Root cause is the layout-computation contract that lives in story 1-9 (root layout computes `currentPath` via `headers().get('x-pathname')` or similar), not in story 1-7.
  evidence: Edge Case Hunter (step-04 iteration 1) flagged that `<Nav>`'s `currentPath === link.href || currentPath.startsWith(link.href + '/')` test fails when the consuming layout (1-9) passes a non-canonical pathname. The safe fix lives in 1-9 (normalize `pathname` before passing it to `<Nav>`); 1-7 owns only the per-link `aria-current` derivation, which is correct as-is for canonical input. Belongs to 1-9's "compute and pass `currentPath`" step.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-implement-persistent-nav-with-brand-mark-links-command-palette-trigger-magnetic-cta-scroll-progress.md`
  summary: The `<MagneticCTA>` size contract reconciliation (`<Nav>` passes `min-h-[44px]`, but `<MagneticCTA>` itself was shipped in 1-6 without an explicit size floor) belongs to story 1-6, not 1-7.
  evidence: Adversarial Reviewer (step-04 iteration 1) flagged that `<Nav>`'s `className="min-h-[44px] px-3 text-sm"` on `<MagneticCTA>` either fights with or silently overrides whatever size contract 1-6 documented. Story 1-7 cannot modify the 1-6 component (Never list). The reconciliation — making 1-6's MagneticCTA document a 44px tap-target floor OR making 1-7's `<Nav>` rely on MagneticCTA's internal sizing — lives in a future touchpoint between 1-6 and 1-9 (likely 1-9, which is the first layout-level mount of both).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-implement-root-layout-applayouttsx-with-semantic-landmarks-skip-to-content-beacons.md`
  summary: The 10th AD-13 client component `<SkipToContent>` (sr-only + `:focus:not-sr-only` reveal to `<main id="main">`) is deferred to a follow-up story to keep this spec under the 1,600-token SCOPE STANDARD budget; evidence: the spec split at step-02 chose the layout + landmarks + beacons as the main goal (FR-19 blocker for 1-11). SkipToContent is the only AD-13 amendment gap remaining after this story; a follow-up story ships `components/client/SkipToContent.tsx` and inserts `<SkipToContent />` as the first body child of the layout. cmdk trigger click-handler wiring is also deferred to the same follow-up story (would require either an 11th client component OR a `<Script>` event delegator; both are scope expansions the narrowed 1-9 explicitly rejects).
