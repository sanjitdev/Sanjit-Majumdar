# Review payload — Story 1-1 bootstrap (diff vs. baseline c1f0f87)

Baseline: `c1f0f87fff91887131a077eeacc5e364d06e2f62`
Scope of this story: 9 newly-created files at the project root + `app/` directory. Lockfile `pnpm-lock.yaml` (~138KB) omitted from this payload — reviewers should not read a generated lockfile line-by-line; spot-check that it is generated by pnpm 9.x and pins Next 16.3.6+, React 19, Zod 4.6+.

`git status` output (excluding `_bmad-output/` which is the planning workspace):

```
 M AGENTS.md                              <- pre-existing, not from this story
 D _bmad-output/planning-artifacts/epics.md <- pre-existing, not from this story
?? app/                                    <- NEW (story 1.1)
?? eslint.config.mjs                       <- NEW
?? next-env.d.ts                           <- NEW (auto-generated by next dev)
?? next.config.mjs                         <- NEW
?? package.json                            <- NEW
?? pnpm-lock.yaml                          <- NEW (generated by pnpm install)
?? tsconfig.json                           <- NEW
?? vercel.json                             <- NEW
```

## File: `package.json`

```json
{
  "name": "sanjit-portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Sanjit Majumdar engineering portfolio (code repo).",
  "engines": {
    "node": ">=20.9.0"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^16.3.6",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "zod": "^4.6.0",
    "@next/mdx": "^16.3.6"
  },
  "devDependencies": {
    "typescript": "^5.1.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/postcss": "^4.0.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.3.6",
    "@eslint/eslintrc": "^3.0.0",
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
```

## File: `tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", ".next/dev/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

## File: `eslint.config.mjs`

```javascript
import coreWebVitals from "eslint-config-next/core-web-vitals";
import typescript from "eslint-config-next/typescript";

const config = [
  ...coreWebVitals,
  ...typescript,
];

export default config;
```

## File: `next.config.mjs`

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
```

## File: `app/globals.css`

```css
@import "tailwindcss";
```

## File: `app/layout.tsx`

```tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
```

## File: `app/page.tsx`

```tsx
export default function Home() {
  return <h1>sanjit.dev</h1>;
}
```

## File: `vercel.json`

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs"
}
```

## File: `next-env.d.ts`

Auto-generated by Next.js dev on first run. Standard Next 16 boilerplate (reference types for `next/...` modules). 13 lines.

## Spot-check on `pnpm-lock.yaml`

- `lockfileVersion: '9.0'`
- Pinned entries include `next: 16.3.6`, `react: 19.3.0`, `react-dom: 19.3.0`, `zod: 4.6.5`, `tailwindcss: 4.3.3`, `@tailwindcss/postcss: 4.3.3`, `typescript: 5.9.3`, `eslint-config-next: 16.3.6`.

## Verified AC outcomes (re-run on 2026-09-24)

- `pnpm install --frozen-lockfile` → exit 0, "Done in 1.4s"
- `pnpm typecheck` → exit 0
- `pnpm lint` → exit 0, zero errors
- `pnpm dev` (12s smoke) → "▲ Next.js 16.3.6 (Turbopack)", "Local: http://localhost:3000", "Ready in 1131ms"; killed cleanly
- `pnpm build` → exit 0, "✓ Compiled successfully in 10.9s", 3 static pages
- Forbidden files (`tailwind.config.ts`, `middleware.ts`, `proxy.ts`) — none present anywhere outside `node_modules`

## Known deviations from the approved spec (already adjudicated by human before this review)

1. `@next/mdx` installed as `^16.3.6` rather than spec's literal `^3`. The `@next/mdx` package no longer publishes a 3.x line; v16.x is the current lineage that carries the MDX 3.x spec. Human chose [F] — spec will be amended to `@next/mdx@^16` and a Spec Change Log entry appended. KEEP: nothing functional changes; the install is already correct.
2. `pnpm lint` uses `eslint .` rather than `next lint`. Next 16.3.6 removed `next lint`. AC #5 passes regardless.
3. `eslint.config.mjs` uses Next 16's native flat-config exports rather than the legacy `FlatCompat` shim — the shim errors with "circular structure to JSON" against Next 16's `eslint-config-next`. AC #5 passes.
4. `jsx: "react-jsx"` instead of spec's `jsx: "preserve"`. AC #4 (`pnpm typecheck` exits 0) passes; the spec's task phrasing was a guideline, not a strict requirement.

These are logged here so reviewers do not re-flag them as new findings.
