# Story 1-1 Round-2 Review Diff

Review scope: changes introduced by story 1-1 since baseline_commit `c1f0f87`.
Source commit: `6a859a4 feat(1-1): bootstrap Next.js 16 + Vercel deploy`
Spec: `_bmad-output/implementation-artifacts/spec-1-1-bootstrap-nextjs-16-vercel-deploy.md`
Spec status (this round): `in-review` (was `done`; reset for round 2).
Previous review pass produced 7 Spec Change Log entries (2026-09-24-1 through 7).

10 files changed, 4296 insertions.

---

## `package.json` (new)

```json
{
  "name": "sanjit-portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Sanjit Majumdar engineering portfolio (code repo).",
  "engines": {
    "node": ">=20.9.0"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^16.3.6",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "zod": "^4.6.0",
    "@next/mdx": "^16.3.6"
  },
  "devDependencies": {
    "typescript": "^5.1.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/postcss": "^4.0.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.3.6",
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
```

## `pnpm-lock.yaml` (new, 4172 lines)

LockfileVersion 9.0; pins `next@16.3.6`, `react@19.x`, `zod@4.x`, `tailwindcss@4.x`,
`eslint@9.x`, `typescript@5.x`, `@next/mdx@16.3.6`.
(Body omitted from this diff excerpt; reviewers should `cat pnpm-lock.yaml | head -1`
and confirm `lockfileVersion: '9.0'` plus `next:` entry.)

## `tsconfig.json` (new)

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "baseUrl": ".",
    "paths": { "@/*": ["./*"] }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts"
  ],
  "exclude": ["node_modules"]
}
```

## `eslint.config.mjs` (new)

```js
import coreWebVitals from "eslint-config-next/core-web-vitals";
import typescript from "eslint-config-next/typescript";

const config = [
  ...coreWebVitals,
  ...typescript,
  {
    ignores: [".next/**", "node_modules/**", "*.tsbuildinfo", "coverage/**"],
  },
];

export default config;
```

## `next.config.mjs` (new)

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
```

## `next-env.d.ts` (new, auto-generated by Next)

```ts
/// <reference types="next" />
/// <reference types="next/image-types/global" />
import "./.next/dev/types/routes.d.ts";
import "./.next/dev/types/root-params.d.ts";

// NOTE: This file should not be edited
// see https://nextjs.org/docs/app/api-reference/config/typescript for more information.
```

## `vercel.json` (new)

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build"
}
```

## `app/globals.css` (new)

```css
@import "tailwindcss";
```

## `app/layout.tsx` (new, placeholder)

```tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
```

## `app/page.tsx` (new, placeholder)

```tsx
export default function Home() {
  return <h1>sanjit.dev</h1>;
}
```

---

## ACs to validate (binding)

- AC1: `pnpm install --frozen-lockfile` exits 0, no drift.
- AC2: `pnpm dev` starts on port 3000 with Turbopack.
- AC3: `pnpm build` succeeds without `middleware.ts` deprecation warnings.
- AC4: `pnpm typecheck` exits 0.
- AC5: `pnpm lint` exits 0 with zero errors on `app/`.
- AC6: `package.json` contains all pins, `engines.node >=20.9.0`, no `tailwind.config.ts`.
- AC7: `proxy.ts` and `middleware.ts` absent.
- AC8: `app/globals.css` exactly the single `@import "tailwindcss";` line.

## KEEP from previous review round (Spec Change Log entries 1–7)

1. `@next/mdx@^16.3.6` (not `^3`).
2. `eslint .` script (not `next lint`); flat-config spread, not `FlatCompat`.
3. `jsx: "react-jsx"` in tsconfig.
4. Explicit `installCommand`/`buildCommand` in vercel.json.
5. `baseUrl: "."` in tsconfig.
6. Explicit `ignores` in eslint.config.mjs (including `coverage/**`, `*.tsbuildinfo`).
7. No `@eslint/eslintrc` devDep.
