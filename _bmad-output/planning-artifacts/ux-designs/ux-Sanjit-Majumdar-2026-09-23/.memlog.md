---
topic: Sanjit Majumdar Engineering Portfolio — UX
updated: 2026-09-23T17:08
updated: 2026-09-23T18:02
updated: 2026-09-23T18:18
updated: 2026-09-23T18:35
updated: 2026-09-23T18:42
updated: 2026-09-23T19:15
---

- (decision) Brand voice: quietly sharp. Dry humor, dry prose. Personality comes through proof-cluster copy and side projects, not the hero. Spine line reads tongue-in-cheek rather than earnest.
- (decision) Visual register: modern-tech-portfolio. Geometric sans, tight grid, solid color blocks, sharp corners. Reads Linear/Vercel-style — modern, tight, no fluff. Personality carried by copy, not decoration.
- (decision) Recruiter-mode treatment: hidden by default. Default site reads engineer-first; recruiter-mode is opt-in via small footer link. Conflicts with PRD UJ-1 (30s recruiter triage) — flag for review.
- (override) Tension w/ PRD UJ-1: hidden recruiter-mode may break the 30s triage journey. Resolution: hidden toggle is acceptable as long as every public route already carries spine line + at least one proof number (per FR-10). Recruiter without the click still sees the spine, proof, and forward-ready layout via FR-10 decentralized-page invariant. UX still surfaces the toggle on every page so it's findable in <5s scan.
- (decision) Numbers tone: hero block (loud). Four proof vectors rendered as a single geometric block directly under the spine line. 4-tile grid on wide, 2x2 on narrow. Numbers become the second thing the eye lands on; spine line is first.
- (decision) now-feed treatment: live ticker. Vertical tick with NOW emphasized at top, latest entry highest. Each entry: dot + title + 1-line description. Honest last-updated stamp. Reads as a status page, not a blog list.
- (decision) Pattern pages: engineering playbook format. Sections: When to use, What it is, How to do it (numbered moves), When NOT to use it (counter-line), Example. Reads like an internal RFC. Counter-line earns its slot because the rest is methodical. Verbatim quote on Ship-Faster Pushback move-4 stays intact.
- (decision) Form factor: responsive web. Desktop, tablet, mobile. No PWA install prompt, no native app. Performance budget: LCP < 1.8s on Slow 4G. Bangladesh 2G edge case addressed via text-first rendering, no heavy assets above the fold.
- (decision) Accessibility floor: WCAG 2.1 AA. Contrast ratios checked in both modes. Keyboard nav on recruiter toggle, forward modal, pattern moves. Semantic landmarks on every route. SR-only labels on proof-number tiles and on the spine line for context.
- (decision) Dark mode: system-preference with manual override. prefers-color-scheme respected by default; toggle in footer (small). Both modes pass WCAG 2.1 AA contrast.
- (decision) Motion: subtle. Entrance fade + 8px slide on scroll for sections, 150ms. Micro-interactions on toggle (100ms color/transform), forward modal (200ms ease-out), recruiter-mode collapse (250ms). prefers-reduced-motion respected; motion removed, layout still works.
- (decision) i18n: English v1, Bengali deferred. Copy stored in en.ts files for future locale split. Bengali flagged as v2 follow-on with no structural rework needed.
- (decision) Personality sections are loud. Games earn their own card category under Lab. Outside-of-production-code section earns real estate on About. How-this-site-is-built architecture diagram becomes its own clickable page (route slash-built). Tension: quietly sharp voice plus loud personality is the signature. Personality comes from content density, not decoration.
- (override) Reconcile: Stitch invented role (Staff/Principal, L6 EQUIV), location (SF Bay Area, US Citizen), client list (KPMG, Vigil, asm), and stack (Rust, ClickHouse, Kafka, CockroachDB). All wrong. Replace with cv.md facts: Senior Software Engineer at Brain Station 23 since Nov 2018, stack C#/.NET/Angular/SQL Server/APIs, located in Dhaka Bangladesh UTC+6. Visual scaffolding from Stitch output stays.
- (change) now entries to rewrite. Stitch invented Ray-marching WebGPU, Wellbook v2 Multi-Region failover drill, Lisp to WASM compiler, Code Review SLAs playbook. Replace with real entries: CityFix, Surakkha, real Lab tools. Vertical ticker visual structure stays.
- (decision) Pattern page title: rename from Canonical Data Model to Canonical Model per spec. Reframe example to multi-platform ERP integration (procurement, finance, vendor portal) instead of multi-tenant clinical. Visual scaffolding stays.
- (decision) Case study: drop HyperLog, Edge Vault Token Broker, LedgeStream, Micro-Xaml Canvas Studio, ZerveVault. All fabricated. Wellbook is the lead case study (oil and gas enterprise project and governance platform per spec). Add 2 more real case studies in v1.5. Visual scaffolding (Patterns Applied rail, Decisions and Tradeoffs table, Measurable Results) is reusable.
- (assumption) Coverage: 5 Stitch surfaces cover homepage, recruiter mode, case study, pattern, now. Missing: built diagram (highest-value 6th surface), lab index, about, projects, forward modal. EXPERIENCE.md documents behavior for all. Mocking built is recommended for v1 follow-up handoff; others can be authored at build-time from spine tables alone.
- (event) spines finalized. DESIGN.md and EXPERIENCE.md both at status final. Reconcile against captured decisions logged. Build-ready.
- (override) Replaced Stitch mocks with ui-ux-pro-max generated mocks. Critique addressed: (1) Stitch had no clear story; new mocks answer who, role, stack, track record above the fold in 5s. (2) Stitch was too dark in dark mode; new direction is light-first warm off-white with paired dark. (3) Stitch had too much information density; new mocks drop mono chrome labels, status beacons on every card, meta-rails. Each UI element earns its slot. Quiet Authority replaces Obsidian Precision as the aesthetic name.
- (override) Full-width redesign. User rejected narrow max-width (760-880px) layouts as too padded; requested full-width, professional look. All 5 mockups rewritten with shared 1440px wrap, 2-col hero (1.4fr/1fr), 4-cell proof strip, sticky nav, dark footer, reduced padding. DESIGN.md tokens (colors, typography, components) unchanged — only container widths and grid proportions moved. Spine remains canonical.
- (override) Premium-spacing refinement. User flagged colliding components and tight spots. Across all 5 mockups: bumped section padding (4rem→5rem, 4.5rem→5.5rem), added inner padding to hero-left/hero-side wrappers, lifted meta-bar inner padding, added separator borders between sidebar rows, added borders under section heads, increased button padding, added top/bottom dividers between hero CTA row and content, increased gaps in 2-col grids (3rem→4rem), added body padding (1.5rem→1.7rem on cards). Mobile breakpoint tightened to remove horizontal padding on collapsed grid cells.
- (override) Compact-padding refinement. User flagged too much padding across the 5 mockups. Applied ~30% reduction: hero padding (4-4.5rem→3rem vertical), section/body padding (5-5.5rem→3.5rem), card padding (1.75-2.25rem→1.25-1.5rem), meta-bar inner padding (1.75rem→1.25rem), footer padding (3rem→2rem), grid gaps (2.5-4rem→2-3rem). Hero name clamp ranges tightened (clamp(2.5-3rem, 5.5-6.5vw, 4-5rem)). Mobile breakpoints scaled proportionally. Visual hierarchy and full-width structure preserved.
- (override) Width-consistency fix on case-study and pattern. User flagged mismatch between centered narrow hero (880px) and full-width meta-bar/pattern-rail/next-nav (1440px). On mock 03 and mock 04: hero switched from `.read` (880px centered) to `.wrap` + new `.hero-inner` (1080px max), and `.read` widened from 880px → 1080px. Result: hero, body sections, and meta-bar/rail/next-nav all share the same proportional column, eliminating the centered-vs-full-width visual tension. Mocks 01/02/05 already consistent.
- (override) Body-section width alignment fix on case-study and pattern. After hero widening, body section content was still off-center because `.read` was capped at 1080px, leaving visible gap between full-width meta-bar/rail (1440px) and body sections. Fixed by widening `.read` to 1440px (matches every other section) and adding a new `.prose` wrapper (max-width 800px) inside each section that needs a reading column. Move-list (mock 04) kept full-width as a deliberate element. Result: body section-num/h2/p now align flush with meta-bar/pattern-rail/next-nav content at the 2rem padding line, no more horizontal jump.
- (override) Applied the same body-section width alignment fix to mock 05 (now). `.read` widened from 1100px → 1440px + `.prose` (800px) added. Ticker entries kept 3-col grid layout at the wider width — the layout naturally accommodates since the middle column absorbs the extra space. Mocks 01 and 02 already full-width throughout (no `.read` containers), no fix needed.
- (override) Impeccable-quality polish pass across all 5 mockups. Changes: (1) Removed unused CSS tokens (`--muted`, `--ring`, `--gap`, `--pad`, `--live-soft` where unused) across all 5 mockups for token consistency. (2) Removed inline `style="..."` on mock 01's `.now-foot a` — replaced with a `.now-foot a` CSS rule with hover underline. (3) Boosted mock 03 `.result-num` from 2.25rem → 2.5rem to restore impact after over-compaction. (4) Improved mock 01 `.hero-spine` margin-top (0.25rem → 1.5rem) for breathing room + added `.hero-bio strong` styling for visual emphasis. (5) Cleaned up mock 02 strip-meta copy (leading-space fix, made grammar cleaner) and added `.strip-meta` margin-left gap. (6) Breadcrumb markup promoted from `<div>` to `<nav aria-label="Breadcrumb">` with `aria-current="page"` on the active page; separators marked `aria-hidden="true"`. (7) Added `focus-visible` outlines to all interactive elements (`.cta`, `.strip-toggle`, `.footer-recruiter`, `.forward-btn`, `.subscribe-input`, `.subscribe-btn`, `.crumb a`) for keyboard accessibility. (8) Added hover state to `.subscribe-btn` (opacity 0.9) for affordance consistency.
- (event) Created 3 new mockup pages to close IA gaps: `06-about.html`, `07-work.html`, `08-built.html`. (1) About: hero + 4 body sections (01 Who I am, 02 Experience left-rail timeline, 03 Outside of production code 2x3 bio-card grid, 04 Get in touch 3-col contact block). Uses `.read` 1440px + `.prose` 800px for reading columns. (2) Work index: 4-cell proof strip + 5 case-study rows in 1.4fr/1fr/200px layout (meta, title+desc+stack tags, result narrative, "CS · 0n →" link). Hover state on rows. (3) Built: 5-cell stack strip + layered architecture diagram (Source/Build/Deliver/Instrument layers with clickable-feeling nodes connected by arrows + legend) + 3-cell detail row (LCP < 1.8s, 8KB JS budget, 100% static routes) + 2-col 6-card decisions grid (Next.js over HTML, CSS Modules over Tailwind, Plausible self-hosted, RSS serverless, "I'd reconsider Sentry", "Dark mode in v1.5"). All 3 pages inherit the same Quiet Authority design system (color tokens, typography, `.wrap` 1440px, focus-visible, mobile breakpoint). Nav `aria-current="page"` set per page. Diagram nodes use hover (border-color shift + translateY -1px) as click affordance even though JS isn't wired — reads as interactive consistently with the aesthetic.
- (override) Impeccable-quality polish pass on the 3 new mockups (06/07/08). (1) Token cleanup: removed unused `--live-soft` from mocks 06 and 08 (was defined but not referenced). (2) Mock 07 (work): added explicit `:focus-visible` on `.work-row` (accent outline + offset 4px + radius 2px so it draws in the gap between rows), and added parent-hover affordance — when a row is hovered or focused, its `.stack-tag` chips shift from muted-fg to fg color and gain a darker border. Smooth 150ms transition on both border-color and color. (3) Mock 08 (built): added `:focus-visible` on `.node` (accent outline + offset 2px + border-color shift) so keyboard users can navigate the diagram; promoted diagram `<div class="arrows">` to `aria-hidden="true"` since the down-arrow and connector text are decorative (the surrounding role="img" + aria-label on `.diagram` carries the meaning); promoted legend from `<div>` to semantic `<ul>`/`<li>` with `list-style: none` reset and decorative swatches marked `aria-hidden="true"`. (4) Mock 06 (about): added `.contact-val a:focus-visible` rule so email/github/linkedin links in the contact block have keyboard outline (was missing — the global `a:focus-visible` rule applies but explicit per-component style is consistent with the rest of the polish pass).
- (branch) v2 design direction spawned from a different brief: **dark theme, strong typography, subtle gradients, minimal animations, generous spacing — high-end developer / product designer portfolio**. Lives at `mockups/v2/` as a parallel branch to the canonical Quiet Authority (light, no gradients) mocks. Aesthetic name: still Quiet Authority but in dark mode with violet/blue gradient accent.
- (decision) v2 color tokens: `--bg: #08090C` near-black with cool blue cast, `--fg: #F5F5F7` warm off-white, single violet accent (`#A78BFA`) lifted to a gradient (`#A78BFA → #60A5FA`) for hero/CTA moments. Single live-green (`#34D399`) for live/shipping indicators. Subtle radial hero gradient (`rgba(167,139,250,0.10)` at top fading to transparent). Cards sit on `--bg-2` (`#0E1014`) with 1px hairline border + a top gradient highlight (1px, transparent→rgba(255,255,255,0.08)→transparent) for the Quiet Authority elevation discipline carried over.
- (decision) v2 typography: Space Grotesk (display, distinctive geometry) + Inter (body, neutral long-form) + JetBrains Mono (technical metadata only). Hero name uses `clamp(2.5rem, 5.5vw, 4.5rem)` with -0.035em letter-spacing. Hero spine uses the gradient text fill. Section H2: `clamp(1.5rem, 2.5vw, 2rem)`.
- (decision) v2 spacing: section padding `7rem` desktop / `4rem` mobile. Container padding `4rem` desktop / `1.5rem` mobile. Card padding `2rem`. Grid gap `1.5rem` between cards, `4rem` between sections in split layouts. Generous everywhere; whitespace carries hierarchy.
- (event) Created v2 mockup set, 9 files in `mockups/v2/`: (1) `00-design-system.md` token reference; (2) `01-homepage.html` hero + selected work 2-col grid + skills 3-col grouped + experience timeline + about/side-projects split row + contact CTA; (3) `02-work.html` filter chips + 5 case-study cards (2-col, 1 spanning); (4) `03-case-study.html` Wellbook lead with sticky sidebar TOC + 5 numbered sections + decisions/tradeoffs 3-col + patterns-cited rail + next-nav; (5) `04-pattern.html` Canonical Model engineering playbook with sticky TOC + 4 numbered moves + counter-line distinctly styled + example box; (6) `05-now.html` live ticker with vertical NOW-first ordering + 6 entries + subscribe block with email form; (7) `06-about.html` bio prose + experience timeline + 6-card personality grid + 3-row contact channels; (8) `07-side-projects.html` 3-col project grid + 2-col games row + open-source 3-row list with star counts; (9) `08-built.html` 5-cell stack strip + 4-layer architecture diagram (Source/Build/Deliver/Instrument) + 3 perf numbers + 6-card decisions grid; (10) `09-recruiter.html` recruiter-mode strip + 2-col hero (name + sidebar contact) + 4-cell proof + forward-to-hiring-manager climax with full-bleed radial glow + 2-card resume section.
- (decision) v2 motion: subtle entrance (no scroll-driven animation in static mocks), 150ms hover transforms on cards (translateY(-2px) or -3px)), pulsing live-dot (2s ease-in-out infinite), radial glow hover on forward-CTA button. `prefers-reduced-motion` honored via the standard CSS media query at the document level (not duplicated in each file's CSS for size).
- (decision) v2 anti-patterns: no emoji as structural icons (only `→`, `↓`, `★`, `●`, `⚠` for universal symbols), no rainbow gradients (single-axis or radial only), no card-mosaic overload (max 3 cols), no skill percentage bars, no testimonial carousels.
- (decision) v2 surface coverage: 9 surfaces (1 more than v1 set) — added `/side-projects` as a dedicated page (v1 had it folded into Lab teaser on homepage). Recruiter-mode still lives at `?for=recruiter` URL param.
- (branch) v3 design direction spawned: **Studio Fusion** — high-end product site × modern dev portfolio × interactive digital studio. Glassmorphism used sparingly. Layered depth. Refined shadows. Confident and intentional. Lives at `mockups/v3/` as a parallel branch to v1 (Quiet Authority, light, editorial) and v2 (Quiet Authority Dark, violet/blue gradient). v3 name: Studio Fusion.
- (decision) v3 color tokens: `--bg: #06070B` pure deep black (darker than v2's #08090C), violet `#A78BFA` + electric cyan `#67E8F9` + tertiary pink `#F472B6` for gradient joins. Glass surface system: `--glass: rgba(255,255,255,0.03)` for elevated panels, `--glass-strong: rgba(255,255,255,0.05)` for featured surfaces. Three-axis gradient-text (`#A78BFA → #67E8F9 → #F472B6`) animated via hueShift keyframe (8s) — used only on display name + proof numbers, never on body text.
- (decision) v3 elevation system: 3-tier (Floor 0 page, Floor 1 glass card with top-gradient highlight, Floor 2 elevated glass with stronger shadow). Five shadows: `--shadow-sm/md/lg/glow/glow-cyan`. Hairline borders at 6%/12% white. Cards sit on glass fill + top-edge gradient highlight + soft drop shadow — depth earned through composition.
- (decision) v3 typography: same stack as v1/v2 (Space Grotesk display, Inter body, JetBrains Mono metadata only). Display sizes expanded (hero name clamp(3.5rem, 8vw, 7rem), head xl clamp(2rem, 4vw, 3.5rem)). Hero tagline is a separate Display-spine role with a custom clamp range.
- (decision) v3 shapes: 5 scale (radius-sm 8px → radius-md 12px → radius-lg 20px → radius-xl 28px → radius-2xl 36px). More generous radii than v1/v2 — the layered-depth language reads better with softer corners.
- (decision) v3 motion: hueShift on gradient text (8s), orbDrift on cursor-reactive hero orb (12s simulated drift), pulse on live-dot (2s), hover lift on cards (250ms cubic-bezier(0.16,1,0.3,1), translateY(-4px) + shadow grow). Magnetic CTAs: simulated via `:hover` translateY(-2px) + brightness 1.05 + glow shadow (real JS magnetic would multiply cursor delta by 0.15). `prefers-reduced-motion` honored at document level (no animations or transitions when set).
- (decision) v3 anti-patterns (carried + extended): no emoji as structural icons (inline SVG + unicode symbols `→`, `↓`, `●`, `★`, `⚠` only); no rainbow gradients (single-axis or 2-stop, with one exception for the 3-stop display gradient which earns its slot); no animation on every element (motion earns its slot — hero orb, gradient text, live dot, hover lift, magnetic CTA only); no card-mosaic overload (max 3 cols, alternating image/body layouts to add rhythm); no glassmorphism on every surface (glass reserved for elevated panels, modals, feature surfaces, sidebars — body backgrounds stay deep-black).
- (decision) v3 hero composition: 2-column with hero-left (name + tagline + CTAs) and hero-right (profile sidebar with avatar, role, key facts) — wider sidebar with richer contact info. Hero includes an animated cursor-reactive orb element (cyan→violet radial blob, drift via CSS keyframe since JS isn't wired). Bold display name uses the 3-stop gradient-text with hueShift. Primary CTA is magnetic (gradient-button fill + glow shadow).
- (event) Created v3 mockup set, 9 files in `mockups/v3/`: (1) `00-design-system.md` token reference; (2) `01-homepage.html` hero + 4-tile impact + 3 alternating 2-col work cards + experience timeline + 6-card expertise grid + lab teaser + now strip + contact; (3) `02-work.html` filter chips + impact strip + 3 case-study rows (alternating image/body) + 2 "coming soon" draft rows; (4) `03-case-study.html` Wellbook lead with sticky sidebar TOC + 6 numbered sections (Context, Problem, Approach, Architecture, Decisions, Results) + tradeoffs table + patterns-cited rail + next-nav; (5) `04-pattern.html` Canonical Model engineering playbook with sticky TOC + 4 numbered moves + warning-styled counter-line ("When NOT to use it") + worked example box; (6) `05-now.html` vertical NOW-first ticker with live pulse dots + 7 dated entries + subscribe form with radial glow; (7) `06-about.html` 2-col hero (bio + profile sidebar) + experience timeline with first/active marker (green pulse) + 3-card philosophy + 6-card personality + 3-card contact; (8) `07-lab.html` 4 featured lab cards with visual previews (gradient mockups) + games row (3 cards) + open-source list (4 rows with stars); (9) `08-built.html` 5-cell stack strip + 4-layer architecture diagram (Source/Build/Deliver/Instrument) with semantic legend + 3-cell perf numbers (LCP/JS/static routes) + 6-card decisions grid including "I'd reconsider Sentry" honest call-out; (10) `09-recruiter.html` recruiter strip + 2-col hero (gradient name + sidebar contact) + 4-cell proof + forward-to-hiring-manager climax with full-bleed radial glow + 2-card resume section.
- (decision) v3 surface coverage: 9 surfaces matching the v2 set exactly — homepage, work index, case study, pattern, now, about, lab, built, recruiter. Same IA closure as v1 and v2; visual register is the only thing that changed. Design system tokens defined in `00-design-system.md` are reused across all 9 files via the same `:root` block.
